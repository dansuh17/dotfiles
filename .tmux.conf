# reload configuration file
bind r source-file ~/.tmux.conf \; display "Reloaded!"
set -g history-limit 99999999  # virtually unlimited scrollback

# map vi movement for pane navigation
bind h select-pane -L
bind j select-pane -D
bind k select-pane -U
bind l select-pane -R

# use vim key bindings in copy mode
setw -g mode-keys vi

# resizing
bind -r H resize-pane -L 5
bind -r J resize-pane -D 5
bind -r K resize-pane -U 5
bind -r L resize-pane -R 5

# uncomment below with right path to open tmux with custom zsh
# ZDOTDIR points to the directory containing custom .zshrc
# set -g default-command "ZDOTDIR=$HOME/.dansuh/ ~/.dansuh/zsh/bin/zsh"
